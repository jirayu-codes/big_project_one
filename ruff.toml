# Project lint settings.
# DTZ rules are ignored because the feature spec explicitly avoids timezone
# handling (SPECS/TECH.md: "Today" comes from the server's local date via
# Python's date, no timezone handling).
[lint]
ignore = ["DTZ"]
